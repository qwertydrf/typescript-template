const list = ['juan', 'aba', 'paralelepipedo', 'aerea', 'avion', 'ojo'];

const isPalindromeList = (list: string[]): boolean[] => {
  return [];
};

const listIsPalindrome = isPalindromeList(list);

export { listIsPalindrome };
